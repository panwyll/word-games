# Database Configuration
# PostgreSQL is required (SQLite doesn't work in serverless environments like Vercel)
# For local development, use PostgreSQL via Docker or a cloud service like Supabase/Neon
# Docker: docker run -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/wordgames"
# For Vercel: Use Vercel Postgres or another PostgreSQL service

# NextAuth Configuration (REQUIRED)
# Generate a random secret: `openssl rand -base64 32`
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Stripe Configuration (OPTIONAL - app works in sandbox mode without these)
# When these are not set, the app runs in "sandbox mode":
# - Stripe payment features are disabled
# - Pricing and account pages show informational messages
# - Word games remain fully functional
# - Users can still authenticate and play all games
#
# To enable Stripe payments, set all three values below:
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_PRICE_ID="price_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."
